
import java.util.*;
public class perfectPairs {

    // min(|x - y|, |x + y|) < min(|x|, |y|)
    // max(|x - y|, |x + y|) > max(|x|, |y|
    public int findPerfectPairs() {

        return 0;
    }



    public static void main(String[] args) {
        perfectPairs o = new perfectPairs();
    }
}
